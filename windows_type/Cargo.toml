[package]
name = "windows_type"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
syn = { version = "3", features = ["derive", "extra-traits", "parsing"] }
quote = "1"
proc-macro2 = "1.0.107"
